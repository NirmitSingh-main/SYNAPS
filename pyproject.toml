[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sih26147-signal-intelligence"
version = "0.1.0"
description = "Signal intelligence analysis for IQ and WAV data."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]